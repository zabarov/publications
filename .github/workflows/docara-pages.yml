name: Publish Docara Site

on:
  # Manual-only after launch. Add a push trigger only when automatic
  # publishing is intentionally desired.
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

env:
  DOCARA_BASE_URL: "https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}"

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: "8.2"
          coverage: none
          tools: composer

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install Composer dependencies
        working-directory: .
        run: composer install --no-interaction --prefer-dist --no-progress

      - name: Initialize Docara
        working-directory: .
        env:
          DOCARA_SKIP_FRONTEND_INSTALL: "true"
        run: php vendor/bin/docara init --update

      - name: Apply publication overrides
        working-directory: .
        run: php scripts/apply-docara-overrides.php

      - name: Sync public publications into Docara source
        working-directory: .
        run: php scripts/sync-publications-to-docara.php

      - name: Clean generated output cache
        working-directory: .
        run: rm -rf .cache build_production

      - name: Install frontend dependencies
        working-directory: .
        run: npm install

      - name: Build frontend assets
        working-directory: .
        run: npm run prod

      - name: Build Docara site
        working-directory: .
        env:
          DOCARA_BASE_URL: ${{ env.DOCARA_BASE_URL }}
        run: php vendor/bin/docara build production

      - name: Adjust GitHub project Pages paths
        env:
          DOCARA_PAGES_PREFIX: "auto"
        run: |
          python3 - <<'PY'
          import os
          import re
          from pathlib import Path

          raw_prefix = os.environ.get("DOCARA_PAGES_PREFIX", "auto").strip()
          if raw_prefix == "auto":
              repository = os.environ.get("GITHUB_REPOSITORY", "")
              repo_name = repository.split("/", 1)[1] if "/" in repository else ""
              prefix = "" if repo_name.endswith(".github.io") else f"/{repo_name}"
          elif raw_prefix in ("", "none", "/"):
              prefix = ""
          else:
              prefix = "/" + raw_prefix.strip("/")

          if not prefix:
              raise SystemExit(0)

          root = Path("build_production")
          patterns = [
              (re.compile(r'(?P<attr>\b(?:href|src|action)=["\'])/(?!/)(?P<path>[^"\']*)'), rf'\g<attr>{prefix}/\g<path>'),
              (re.compile(r'(?P<attr>\bcontent=["\'])/(?!/)(?P<path>[^"\']*)'), rf'\g<attr>{prefix}/\g<path>'),
          ]
          for path in list(root.rglob("*.html")) + list(root.rglob("*.json")) + list(root.rglob("*.js")):
              text = path.read_text(encoding="utf-8")
              updated = text
              for pattern, replacement in patterns:
                  updated = pattern.sub(replacement, updated)
              updated = updated.replace('fetch(`/search-index_', f'fetch(`{prefix}/search-index_')
              updated = updated.replace('fetch("/search-index_', f'fetch("{prefix}/search-index_')
              updated = updated.replace("fetch('/search-index_", f"fetch('{prefix}/search-index_")
              updated = updated.replace('window.location.replace("', f'window.location.replace("{prefix}/')
              updated = updated.replace('window.location.replace(`', f'window.location.replace(`{prefix}/')
              updated = updated.replace('"url": "\/', f'"url": "\/{prefix.strip("/")}/')
              duplicate_locale = re.compile(r'(?P<root>' + re.escape(prefix) + r')?/(?P<locale>[a-z]{2}(?:-[a-z]{2})?)/(?P=locale)(?=[/"?#])')
              updated = duplicate_locale.sub(lambda match: f'{match.group("root") or ""}/{match.group("locale")}', updated)
              if updated != text:
                  path.write_text(updated, encoding="utf-8")
          PY

      - name: Disable Jekyll
        run: touch build_production/.nojekyll

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: build_production

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
