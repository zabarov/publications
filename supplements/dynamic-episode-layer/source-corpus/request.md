# Fixture Request

Prepare an implementation plan for a modular authentication package that adds
API token issuance, token revocation and audit logging.

The plan must identify:

- package boundaries;
- required data objects;
- public API endpoints;
- tests;
- security and audit constraints;
- implementation order;
- assumptions that need review.

The same request is used for the baseline run and the graph-controlled run.

